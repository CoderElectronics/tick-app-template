# tick-engine App Template

App template for tick-engine. Tick is the lightweight VM/runtime that powers apps and watch faces inside of Mirror OS and allows for low-power operation in cpu-time-sparse programs.
